export const initialState = {user: null}

export const actionTypes = {
    SET_USER: "SET_USER"
}

const reducer = (state, actions)=>{
    console.log(action)
    switch(action.type) {
        
    }
}